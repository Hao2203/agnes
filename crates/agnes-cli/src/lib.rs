//! Internal helpers for the `agnes` binary. Exposed as a library so
//! integration tests can hit them.
pub mod chat;
pub mod cli;
pub mod input;
pub mod plan_view;
pub mod run_cmd;
pub mod sink_stderr;
